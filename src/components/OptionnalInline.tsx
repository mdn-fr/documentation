import { JSX } from "react"

const OptionnalInline = (): JSX.Element => (
  <div className={'badge-inline'}>
    <span className={'badge-inline__label'}>Facultatif</span>
  </div>
)

export { OptionnalInline }
