import { JSX } from "react"

const ReadOnlyInline = (): JSX.Element => (
  <div className={'badge-inline'}>
    <span className={'badge-inline__label'}>Lecture seule</span>
  </div>
)

export { ReadOnlyInline }
